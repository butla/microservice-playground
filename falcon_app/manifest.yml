---
applications:
- name: falcon-test
  command: uwsgi --http :$VCAP_APP_PORT --module app:app --enable-threads --disable-logging
  memory: 128M
  buildpack: python_buildpack 
  env:
    # LOG_LEVEL isn't used yet
    LOG_LEVEL: "INFO"
    VERSION: "0.0.1"
